[project]
name = "jamjet-{{framework}}-integration"
version = "0.1.0"
description = "{{Framework}} × JamJet integration example"
requires-python = ">=3.11"
dependencies = [
    "jamjet>=0.3.1",
    # "{{framework}}>={{X.Y.Z}}",
    # "openai>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
